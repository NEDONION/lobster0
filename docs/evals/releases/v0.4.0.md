# MiniClaw Eval Record v0.4.0

> 日期：2026-08-08
> 阶段：Phase 4 Feishu Channel
> 结论：implementation gate PASS；live Feishu gate PENDING

## 可复现门禁

```bash
MINICLAW_NODE=/absolute/path/to/node-22-or-newer \
  uv run python -m unittest discover -s tests -v
pnpm --dir tui test
uv run ruff check .
uv run miniclaw eval run --suite all --root evals/scenarios
uv build
git diff --check
```

| 门禁 | 结果 |
| --- | --- |
| Python tests | 382/382 PASS |
| TypeScript tests | 25/25 PASS |
| active Agent cases | 24/24 PASS |
| Feishu Channel cases | 12/12 PASS |
| Ruff | PASS |
| 真实 WebSocket / 20 轮 / restart / reconnect | PENDING |

实现证据覆盖 strict config、schema v2、Adapter、message ID 幂等 Inbox、有限 Worker、Unicode 分片、durable
Outbox、Typing/进度卡、Owner 审批、official WebSocket Transport、Gateway 生命周期与 13 项 offline Doctor。

## Live 为什么待完成

本次验证时，仓库私密 `.env` 只有模型 Key，没有 `MINICLAW_FEISHU_APP_ID` / App Secret；本地
`config.toml` 也未开启 Channel。因此不能建立真实连接或验证租户权限。准备好企业应用后运行：

```bash
uv run python scripts/feishu_live_smoke.py --confirm-live
```

再补录日期、commit、domain、pass/fail/skip 和 reviewer。禁止记录 Secret、token、完整 Open ID/Chat ID/
Message ID、消息正文或私人截图。在此之前不能标记 production verified。
