# MiniClaw Eval Record v0.5.3 Core

> 日期：2026-08-09
>
> 交付：v0.5.3 Feishu / Discord Live Gate Core hardening
>
> 结论：**CORE IMPLEMENTATION PASS / FEISHU 15-CASE LIVE PENDING / DISCORD LIVE PENDING**

本记录只固定已经进入 `main` 的本地实现和确定性门禁。它不声称 Feishu 或 Discord 已完成严格 15/15
真实平台验收；PID、平台 ID、消息正文、用户名、本地路径和私人 Evidence 均不写入仓库。

## 1. 版本身份

| 项目 | 结果 |
| --- | --- |
| Core implementation commit | `f856679` |
| Gate/document baseline commit | `54db7b0` |
| Python | 562/562 PASS |
| TypeScript | 30/30 PASS |
| Offline Agent | 29/29 PASS |
| Channel | 32/32 PASS |
| Channel local soak | 20 轮、640/640 PASS |
| Ruff / docs / diff | PASS |
| Feishu | Owner DM Delivery verified；strict 15-case LIVE PENDING |
| Discord | IMPLEMENTATION PASS / LIVE PENDING |
| Telegram | IMPLEMENTATION PASS / LIVE PENDING |

## 2. 已进入 Core 的能力

- Feishu SDK handler 的 WebSocket query、Ticket、Token、Authorization 与异常文本脱敏；
- 每个 `MINICLAW_HOME` 的单 Gateway lease，以及 commit/PID/UTC 启动时间 provenance；
- Feishu 与 Discord Live Harness 共用的受管 Gateway 子进程、精确 ready marker、有界日志和优雅停止；
- Live Evidence 对 clean commit、Gateway 来源、skip/fail、secret match 和 schema 的失败关闭；
- 对异常退出时孤立 Tool 消息的可恢复历史处理，避免下一次 Provider 请求得到非法 Tool transcript。

以上能力由 sentinel Secret、重复 lease、旧进程、dirty worktree、commit mismatch、异常退出和 history recovery
回归覆盖。测试只使用合成数据，不读取真实 `.env`、Token、消息正文或私人 ID。

## 3. 尚未完成的 Live Evidence

| Gate | 当前状态 | 升级为 Live PASS 的条件 |
| --- | --- | --- |
| Feishu strict suite | `15-CASE LIVE PENDING` | 固定 15 项全部 pass、0 fail、0 skip、secret scan 0、Gateway commit/graceful 一致。 |
| Discord strict suite | `LIVE PENDING` | 私有测试 Server 的 15 项全部 pass，并满足同样的来源与隐私约束。 |
| Cross-platform isolation | `LIVE PENDING` | Feishu/Discord 同时启用时，重连和故障不跨 pipeline 传播。 |
| Long-running soak | `LIVE PENDING` | 真实常驻环境完成约定时长且没有秘密泄露、重复执行或丢失恢复。 |

因此 v0.5.3 目前只能写 **Core Implementation Pass**，不能写 `Feishu 15/15 LIVE PASS`、
`Discord 15/15 LIVE PASS` 或 `production verified`。

## 4. 可复现本地门禁

```bash
uv run python -m unittest discover -s tests -v
pnpm --dir tui test
pnpm --dir tui build
uv run miniclaw eval run --suite offline --root evals/scenarios
uv run miniclaw eval run --suite channel --root evals/scenarios
uv run miniclaw eval run --suite channel --repeat 20 --json --root evals/scenarios
uv run ruff check .
uv run python scripts/validate_docs.py
git diff --check
```

严格 Live Gate 的执行、失败处理和 Evidence schema 见
[v0.5.3 实施计划](../../superpowers/plans/2026-08-09-phase-5-3-feishu-discord-live-gate.md)。
