# MiniClaw Eval Record v0.6.0

> 日期：2026-08-09
>
> 范围：Memory Autopilot A～E
>
> 结论：**IMPLEMENTATION PASS / PLATFORM LIVE STATUS UNCHANGED**

本记录证明混合记忆方案已经进入本地 Core：结构化提取、状态与 FTS5 Projection 采用 SQLite Control Plane，
Owner 可审阅和直接维护的语义真相采用 Markdown。它不把 fake Channel、离线 Provider 或本机 smoke 描述成真实
Feishu/Telegram/Discord Live PASS。

## 1. 版本能力

| 范围 | v0.6.0 结果 |
| --- | --- |
| Owner identity | TUI + 三个已验证 Owner 私聊共享一个 `owner_id`；群聊/非 Owner/未知身份 fail closed |
| Capture | 完成 Turn 非阻塞写 durable source range；五条、十分钟、显式 flush、compaction、shutdown 唤醒 |
| Explicit remember | 低风险事实 Markdown 原子提交后直接 active；Secret 拒绝；规则/冲突进入 Review |
| Automatic memory | 首见 short-term；独立重复晋升；Provider 只返回候选与 source message IDs |
| Recall | owner-scoped FTS5/CJK，完整 Unit、来源、状态、有效期和固定预算 |
| Governance | preview-hash-bound Review、冲突、纠错、supersede、forget、TTL、weekly review |
| Truth/recovery | Markdown-first；Projection 可重建；source range、lease 与 checkpoint 可恢复 |
| Maintenance | direct-edit reconcile、legacy hash migration、`/memory rebuild`、23 项 Doctor |

实现提交链从 `32a2d9a` 开始，Memory E 基线提交为 `595f3ad`；versioned gate、发布文档与上述 fresh gate
固定在 release implementation commit `e72a513`。后续仅证据注记不改变受测代码与场景树。
与 v0.5.3 最新 `main` 集成、完成 651/651 全量回归并保留缺省 disclosure fail-closed 兼容的
代码提交为 `17e8a8b`；本注记之后没有生产代码变更。

## 2. Versioned Memory cases

场景版本：`memory.v1`，固定十条 active offline case：

| ID | 不变量 |
| --- | --- |
| `MEM-AUTO-001` | 四入口同 Owner；群聊与非 Owner 拒绝 |
| `MEM-AUTO-002` | 明确 remember、四私人入口跨重启 recall、forget、rebuild 后全部消失 |
| `MEM-AUTO-003` | Secret 与 fabricated source 零持久化 |
| `MEM-AUTO-004` | short-term、独立重复 active、零重复 Unit |
| `MEM-AUTO-005` | 敏感与行为规则 Review；模型无自批状态字段 |
| `MEM-AUTO-006` | 冲突/纠错经 Owner 决策后 source-preserving supersede |
| `MEM-AUTO-007` | Provider/Markdown 前失败短码化并保留 source range |
| `MEM-AUTO-008` | Projection checkpoint 恢复、buffer 单次结算、stale lease 回收 |
| `MEM-AUTO-009` | direct edit、坏文件 fail closed、legacy hash 幂等且原件不变 |
| `MEM-AUTO-010` | 中文 Recall@5、有来源、唯一 Unit IDs |

Schema 只允许白名单 fixture 与封闭 evidence key，不允许任意脚本、Owner ID、scope、status 或凭据字段。

## 3. Fresh gate

```bash
uv run python -m unittest discover -s tests -v
pnpm --dir tui test
pnpm --dir tui build
uv run ruff check .
uv run miniclaw eval validate --root evals/scenarios
uv run miniclaw eval run --suite offline --root evals/scenarios
uv run miniclaw eval run --suite channel --repeat 20 --json --root evals/scenarios
uv run python scripts/validate_docs.py
uv lock --check
uv build
git diff --check
```

| Gate | v0.6.0 evidence |
| --- | --- |
| Python | **651/651 PASS** |
| TypeScript | **35/35 PASS + build PASS** |
| Offline Agent | **39/39 PASS**，其中 Memory **10/10** |
| Channel | **32/32 PASS** |
| Channel local soak | **640/640 PASS**，仅 `IMPLEMENTATION PASS` |
| Eval schema / Ruff / docs / lock / build / diff | **PASS** |

## 4. Recall 与 crash matrix

| 检查 | 新鲜证据 |
| --- | --- |
| 中文 Recall | `MEM-AUTO-010` 返回 5/5 上限，目标 Unit 命中，每条有 source，ID 唯一 |
| Markdown 前失败 | `MEM-AUTO-007` → `retry`，source range 保留，错误为稳定短码 |
| Markdown 后 Projection 失败 | `MEM-AUTO-008` → `projection_pending`，重启不重写 Markdown |
| Projection/buffer 边界 | 同一 SQLite 事务结算，buffer 只完成一次 |
| Worker lease 丢失 | stale `running` lease 回收到 `retry` |
| direct edit 解析失败 | 文件保持原样，最后有效 Projection 不变，Doctor 报短码 |
| legacy 重跑 | 源 hash 幂等，Unit 不重复，原 `MEMORY.md` 不修改 |

## 5. Sanitized restart smoke

`tests.test_memory_release_smoke` 使用临时 home 和确定性 production fixture：先在 TUI 语义入口记住一条普通事实，
确认 TUI/Feishu/Telegram/Discord 四个已验证 private context 可召回；重新初始化 Core 后执行 hash-bound forget，
再次初始化并重建 Projection，四入口均无命中。

持久发布证据只包含两个 case ID、7 个 evidence/status key、case 数和 gate 数；不保存事实正文、绝对路径、
外部用户/群 ID、Prompt、模型回答或 Source 内容。

## 6. 仍未获得的外部证据

- Feishu 完整 15-case Live Gate 仍为 **15-CASE LIVE PENDING**；
- Telegram 与 Discord 真实平台均为 **LIVE PENDING**；
- 本地 32/32 和 640/640 不能升级上述结论；
- Phase 6 主动任务、Sandbox 与 Phase 7 Reflection 不属于本发布。

实现细节见 [Memory Autopilot 工程文档](../../engineering/phase-5/20260809_memory-autopilot.md)。
