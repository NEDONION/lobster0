# SDD ledger — plan: docs/superpowers/specs/2026-08-10-miniclaw-to-lobster0-rename-design.md
Preflight: isolated worktree .worktrees/rename-lobster0 on codex/rename-lobster0, branched from origin/main at 274cdf6.
Design: user confirmed full rename (CLI/PyPI/import/state-dir/env-vars/npm scopes/docs/website) to lobster0, not just URL fix. No public release shipped yet, so no back-compat migration needed. Design doc committed.

## Execution (this session)

1. Recomputed scope: `git ls-files` = 643 tracked files; case-insensitive `miniclaw` content grep = 516 files / 6314 occurrences (design doc's 515/6271 estimate, small drift expected).
2. `git mv src/miniclaw src/lobster0` first. Also `git mv`'d 3 docs/assets/*.png TUI screenshots and 8 dated docs/superpowers/plans|specs/*.md files (miniclaw-* -> lobster0-*) for full filename consistency — these aren't git-history-frozen, they're living docs per the design doc's own reasoning.
3. Wrote a case-variant-aware substitution script (order: MINICLAW, MiniClaw, Miniclaw, miniclaw -> LOBSTER0/Lobster0/Lobster0/lobster0), tested on a sample (paths.py, cli.py) first, then ran over the full tracked-file list.
   - **Bug caught and fixed**: first pass silently skipped 12 files with non-ASCII (Chinese) filenames because `git ls-files` quotes them by default (core.quotepath) and my script's file list had the escaped/quoted literal strings, not real paths -> `FileNotFoundError` silently swallowed. Fixed by setting `git config core.quotepath false` and regenerating the file list; re-ran the substitution, all 12 caught up correctly on the second pass.
   - **Gap caught and fixed**: the design doc's case-variant table (MINICLAW/MiniClaw/Miniclaw/miniclaw) doesn't cover the *hyphenated* GitHub repo slug `mini-claw` (no "i" between mini and claw when read as identifier boundary — it's a distinct token). Found 106 occurrences across 30 files, 100% of them part of `github.com/NEDONION/mini-claw` URLs (repo URL, clone commands, release API/download URLs, the manifest schema $id, and a `segments[1:5] == ["NEDONION","mini-claw",...]` check in src/lobster0/install/models.py). Added a second substitution pass: `mini-claw -> lobster0` (whole-slug replacement, not `mini-lobster0`), re-ran, re-verified install test suite (256 tests) still green.
4. Deliberate exception: left `docs/superpowers/specs/2026-08-10-miniclaw-to-lobster0-rename-design.md` (filename AND content) completely untouched. It's the design record of this exact rename and its own before/after table uses literal "MiniClaw"/"mini-claw" as the *historical* value being described — blind substitution would make both table columns read identically and destroy the document's own meaning. Confirmed no other tracked file references its path, so nothing broke by not renaming it.
5. Hand-verified pyproject.toml (name, [project.scripts], packages.find, dynamic version, package-data), and package.json in tui/desktop/website/browser-worker — all correct.
6. Environment gap found and resolved locally (not part of any commit, gitignored build output): a fresh `browser-worker/dist` didn't exist in this worktree (never had `pnpm install`/build run here), causing `BROWSER-017` (profile_lock fixture) to fail with `execution_error` and cascading into 2 more CLI eval test failures — this was NOT a rename regression, just a missing local build artifact. Rebuilt it locally via `tsc` (borrowed node_modules from the sibling non-worktree checkout, ran `tsc -p tsconfig.json`, then removed the borrowed node_modules, kept only the resulting gitignored `dist/`). Confirms clean against the renamed source. Did NOT attempt this for tui/desktop/website: `pnpm install` there hits the same environment gap as the acknowledged `TuiBundleTest` issue (local pnpm 7.29.2 vs `packageManager: pnpm@10.14.0` pinned, plus an unrelated npmmirror-registry client incompatibility `ERR_INVALID_THIS`) — confirmed by trying, then backed out untracked node_modules copies to avoid misleading state.
7. Full Python suite: `uv run python -m unittest discover -s tests -q` -> 1316 tests, only `TuiBundleTest.setUpClass` fails (known, pnpm/Node floor, unrelated to rename) + 1 skip. Ruff clean. `scripts/validate_docs.py` -> PASS.
8. Final sweep: zero remaining `miniclaw`/`mini-claw` hits outside the one deliberate exception file. Checked for doubled-substitution artifacts (LOBSTER0LOBSTER0 etc.) — none. Spot-checked remaining "claw" hits (OpenClaw, ClawTrace, "Claw Trail") — all genuinely unrelated (OpenClaw is a distinct third-party reference project; ClawTrace/Claw Trail are pre-existing UI feature names, thematically fine to keep given the new brand is literally a lobster).
9. Committed in 2 commits (both leave the Python suite green):
   - `3ca8e57` refactor(rename): 将 miniclaw 包与 CLI 迁移为 lobster0 — src/, tests/, evals/, scripts/, pyproject.toml, uv.lock, requirements-all.lock, .env.example, release/. 337 files, +2695/-2695.
   - `e712e9a` docs(rename): 文档、官网内容与桌面/TUI/Browser-Worker 品牌文案改为 Lobster0 — docs/, README*, LICENSE, AGENTS.md, .gitignore, start-desktop.command, website/, desktop/, tui/, browser-worker/. 222 files, +3171/-3171.
   - Note: the earlier `git mv` of the 3 png + 8 md doc files landed inside commit 1 (their path move was already staged from step 2, before I'd staged the content substitution for the docs bucket) — harmless: commit 1 has them at the new path with old (pre-substitution) content, commit 2 finishes their content update. Doesn't affect Python test correctness either way.
10. Noticed mid-task that `origin/main` advanced 13 commits (274cdf6 -> 601efc1) from unrelated concurrent work (a phase-6 Feishu production merge + website changes) while this worktree was active. `git diff --stat origin/main` therefore now looks inflated/confusing (conflates unrelated upstream churn with the rename). The accurate scope-of-rename diff is `git diff --stat 274cdf6 HEAD` = 552 files, +5918/-5866 (274cdf6 is the actual commit this branch forked from, confirmed via `git merge-base`).
11. Did not push, did not rebase onto the new origin/main, did not open a PR — left for the reviewing session per instructions.
